[project]
name = "plant"
version = "0.1.0"
description = "A lightweight, Modbus-based industrial control environment used to emulate a water tank, pump, and valve system."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pymodbus>=3.15.0",
]
